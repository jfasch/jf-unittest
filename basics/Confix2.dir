# -*- python -*-

h = [
    'test_case.h',
    'test_result.h',
    'test_result_fwd.h',
    'test_suite.h',
    ]

cc = [
    'test_case.cc',
    'test_suite.cc',
    ]

LIBRARY(members=[H(filename=f, install=['jf', 'unittest']) for f in h] + [CXX(filename=f) for f in cc])
