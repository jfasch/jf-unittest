# -*- python -*-

DIRECTORY(['tests'])

h = [
    'failure.h',
    'test.h',
    'test_case.h',
    'test_case_fwd.h',
    'test_result.h',
    'test_result_fwd.h',
    'test_suite.h',
    'test_suite_fwd.h',
    ]

cc = [
    'test_case.cc',
    'test_suite.cc',
    ]

LIBRARY(members=[H(filename=f, install=['jf', 'unittest']) for f in h] + [CXX(filename=f) for f in cc])
